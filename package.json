{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build":"npm run build"
  },
  "dependencies": {
    "@azure/static-web-apps-cli": "^1.1.6",
    "npm": "^8.11.0"
  }
}
